name: Snowball Transactions Monitor

on:
  schedule:
    - cron: '*/30 * * * *'  # Проверка каждые 30 минут
  workflow_dispatch:         # Возможность запустить вручную

permissions:
  contents: write  # Нужно для сохранения истории сделок

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libnss3 \
            libnspr4 \
            libatk1.0-0 \
            libatk-bridge2.0-0 \
            libcups2 \
            libdrm2 \
            libxkbcommon0 \
            libxcomposite1 \
            libxdamage1 \
            libxrandr2 \
            libgbm1 \
            libasound2t64

      - name: Install Python dependencies
        run: |
          pip install telethon playwright
          playwright install chromium

      - name: Restore transaction history
        run: |
          if [ -f snowball_transactions.json ]; then
            echo "✅ История сделок загружена."
          else
            echo "[]" > snowball_transactions.json
          fi

      - name: Check for new transactions
        env:
          PHONE: ${{ secrets.PHONE }}
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
        run: python monitor_snowball.py

      - name: Save transaction history
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update snowball transaction history"
          file_pattern: snowball_transactions.json
